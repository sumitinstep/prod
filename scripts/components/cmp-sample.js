(function($)
{
	var cmpSample = function()
	{
		$(document).ready(function()
		{
			var cmp = $('.cmp-sample');
			if (cmp.length)
			{
				cmp.each(function()
				{
				});
			}
		});
	}();
})(jQuery);